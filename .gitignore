node_modules
.DS_store

/build
/.react-router
/public/build
/server-build
.env

/prisma/data.db
/prisma/data.db-journal
/prisma/data.db-wal
/prisma/data.db-shm
/backups/
/tests/prisma

/test-results/
/playwright-report/
/playwright/.cache/
/tests/fixtures/email/
/coverage

/other/cache.db

# Easy way to create temporary files/folders that won't accidentally be added to git
*.local.*

# Logs — may contain access tokens, request data, or stack traces
*.log

# Local / deployment env files with secrets (the checked-in .env.example is kept)
.env.*
!.env.example
